class MissingNumber:
    def missingNumber(self, nums: List[int]) -> int:
        mySet = set(nums)
        for i in range(len(nums)):
            if i not in mySet:
                return i
        return i + 1
